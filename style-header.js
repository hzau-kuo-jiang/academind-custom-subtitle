function styleHeaderMain() {
  const headerEl = document.querySelector(".full-width.half-height");

  headerEl.style.bottom = "0";
  headerEl.style.top = "initial";
  headerEl.style.position = "absolute";
}

styleHeaderMain();
